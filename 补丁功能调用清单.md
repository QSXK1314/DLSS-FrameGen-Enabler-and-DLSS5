# 补丁功能调用清单（重写参考 - 最终版）

## 一、多帧生成功能

### 1. 经典模式（RTX40系，老驱动/310.8帧生成）
**补丁来源**：`老驱动多帧生成\核心\`
**安装位置**：
- `nvngx_dlssg.dll`（310.8）→ 替换游戏原文件（Engine目录下）
- `RTX40MFG.asi` → 游戏exe目录
- `RTX40MFG_config.json` → 游戏exe目录
- `version.dll` → 游戏exe目录
- 不需要 reshade

### 2. 高级模式（RTX40系，新驱动/310.9帧生成）
**补丁来源**：`40系原生多帧生成\`
**安装位置**：
- `nvngx_dlssg.dll`（310.9.0）→ 替换游戏原文件（Engine目录下）
- `nvngx_dlss.dll` → 游戏exe目录（从排错目录取）
- `version.dll` → 游戏exe目录
- `reshade\`（dxgi.dll, ReShade.ini, ReShade.log, ReShadePreset.ini）→ 游戏exe目录
- `Universal-RTX-40-MFG-Unlock-v1.2.1\`所有文件 → 游戏exe目录
- `global.ini` 重命名为 `version.ini`
- reshade-shaders 目录 → 游戏exe目录（从 dlss5 reshade配置页面 取）

### 3. RTX20系多帧生成
**补丁来源**：`20系多帧生成\`
**安装位置**：游戏exe目录
- 根目录文件（dinput8.dll, dlssg_sm86.ini, version.dll）→ 游戏exe目录
- `runtime\` 整个子目录 → 游戏exe目录（保持runtime子目录结构）
- 不需要 reshade
- 没有重命名操作

### 4. RTX30系多帧生成
**补丁来源**：`30系多帧生成\`
**安装位置**：游戏exe目录
- 所有文件（dlssg_sm86.ini, version.dll）→ 游戏exe目录
- 不需要 reshade
- 没有重命名操作

### 5. RE引擎多帧生成
**补丁来源**：`RE引擎多帧生成\` + `RE框架\`
**安装位置**：游戏根目录（RE引擎exe在根目录）
- `RE引擎多帧生成\`所有文件（dlssg_sm86.ini, nvngx_dlss.dll, nvngx_dlssg.dll, version.dll）→ 游戏根目录
- `RE框架\dinput8.dll` → 游戏根目录
- 不需要 reshade
- 没有重命名操作

### 6. 2077专用多帧生成
**补丁来源**：`2077专用多帧生成补丁\`
**安装位置**：游戏 `bin\x64` 目录
- 根目录所有文件 → bin\x64
- `plugins\` 整个目录（含子目录）→ bin\x64\plugins\
- 安装后弹出使用说明

---

## 二、DLSS5功能

### 7. 通用DLSS5（N卡）
**补丁来源**：
- `dlss5 reshade配置页面\`（reshade基础全部文件）
- `N卡DLSS5补丁\`（nvngx_dlssnr.dll, renodx-dlss5.addon64）
**安装位置**：游戏exe目录（Binaries\Win64）
- 所有文件直接放到游戏exe目录

### 8. 通用DLSS5（A卡，仅7000/9000系）
**补丁来源**：
- `dlss5 reshade配置页面\`（reshade基础）
- `A卡DLSS5补丁\`（nvngx_dlssnr.dll, dlssnr_on_amd_setup.exe）
**安装位置**：游戏exe目录
- 安装后自动运行 dlssnr_on_amd_setup.exe
- 提示：需要开启FSR3.0以上，不要选FSR2.0

### 9. RE引擎DLSS5
**补丁来源**：
- `dlss5 reshade配置页面\`（reshade基础）
- `RE框架\dinput8.dll`
- `RE引擎通用dlss5补丁\DLSS5 on RE\`（nvngx_dlss.dll, nvngx_dlssnr.dll, RE_DLSS5_Core.dll, RE_DLSS5_Core_settings.json, version.dll）
**安装位置**：游戏根目录
- 注意：开启DLSS5后无法开启游戏自带帧生成

### 10. 燕云十六声DLSS5（特殊适配）
**补丁来源**：`燕云十六声DLSS5补丁\`
**安装位置**：
- 游戏exe目录（`yysls_medium\Engine\Binaries\Win64r`）：dxgi.dll, ReShade.ini, ReShadePreset.ini, reshade-shaders\, D3DCompiler_47.dll, nvngx_dlssnr.dll, renodx-dlss5.addon64
- Streamline目录：nvngx_dlss.dll, nvngx_dlssg.dll, sl.*.dll等全部文件
- 注意：虽然路径包含 Engine，但 Win64r 确实是游戏真正运行exe的目录

---

## 三、DX9 DLSS5功能

### 11. DX9 32位 DLSS5
**补丁来源**：`dx9 32位dlss5补丁\`
**安装位置**：
- 游戏exe目录：D3D9.dll, dgVoodoo.conf, dgVoodooCpl.exe, dlss5-feed.addon32, dxgi.dll, ReShade.ini, ReShade.log, ReShadePreset.ini, reshade-shaders\
- `host64\` 子目录：dlss5-feed-host64.exe, dxgi.dll, nvngx_dlss.dll, nvngx_dlssnr.dll, renodx-dlss5.addon64
- 注意：dgVoodoo三文件可能需要放到dll运行目录（如bin目录）

### 12. DX9 64位 DLSS5
**补丁来源**：`dx9 64位dlss5补丁\`
**安装位置**：游戏exe目录
- 所有文件（D3D9.dll, dgVoodoo.conf, dgVoodooCpl.exe, dlss5-feed.addon64, dxgi.dll, nvngx_dlss.dll, nvngx_dlssnr.dll, renodx-dlss5.addon64, ReShade.ini, ReShadePreset.ini, reshade-shaders\）
- 不需要host64目录

---

## 四、排错功能

### 13. 经典模式排错
**补丁来源**：`老驱动多帧生成\排错\`
**规则**：只替换游戏目录里已有的 sl. 开头文件，不添加新文件

### 14. 高级模式排错
**补丁来源**：`40系原生多帧生成\排错\`
**规则**：同上

---

## 五、游戏识别规则

### 游戏真正运行exe目录
- 普通UE游戏：`游戏名\Binaries\Win64`（排除 Engine\Binaries\Win64）
- RE引擎游戏：游戏根目录
- 2077：`bin\x64`
- 异环：`Client\WindowsNoEditor\HT\Binaries\Win64`
- 燕云十六声：`yysls_medium\Engine\Binaries\Win64r`（注意：虽然包含Engine，但确实是游戏exe目录）
- DX9游戏：用户手动选择

### nvngx_dlssg.dll位置
- 可以在Engine目录下（如 Engine\Plugins\Runtime\Nvidia\StreamlineCore\Binaries\ThirdParty\Win64）
- 搜索整个游戏目录找到即替换

### 其他补丁位置
- 除nvngx_dlssg.dll外，所有补丁必须放到游戏真正运行exe目录
- 绝对不能放到 Engine\Binaries\Win64
